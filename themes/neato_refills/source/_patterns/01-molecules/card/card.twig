<div class="card">
  <div class="card-image">
    <img src="{{ img_src }}" alt="{{ img_alt }}">
  </div>
  <div class="card-header">
    {# todo - we should actually be including the appropriate field molecule here #}
    {{ header }}
  </div>
  <div class="card-copy">
    <p>{{ copy }}</p>
  </div>
</div>